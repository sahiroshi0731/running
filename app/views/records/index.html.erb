<div class='notice'>
  <%= notice %>
</div>
<div class='topic'>
  今月は、<%= @records.count %>件の記録があります</br>
  月間走行距離は<%= @total_distance %>kmです 
</div>
<%= link_to "登録する", "records/new", method: :get ,class: "btn-square-so-pop" %>
<div class='list'>
  <table>
    <tr>
      <th>日付</th>
      <th>タイトル</th>
      <th>距離</th>
      <th>タイム</th>
    </tr>
    <% @records.each do |record| %>
      <% day = l record.run_day %>
      <tr>
        <td><%= link_to day, "/records/#{record.id}", method: :get %></td>
        <td><%= record.run_title %></td>
        <td align="right"><%= record.run_distance %>km</td>
        <td><%= l record.run_time %></td>
    </tr>
    <% end %>
  </table>
</div>

  
  
  
  
